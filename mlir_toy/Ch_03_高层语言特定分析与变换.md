# 第3章：高层语言特定的分析与变换

[TOC]

创建与输入语言语义紧密匹配的方言，可以在 MLIR 中实现那些需要高层语言信息并且通常在语言 AST 上执行的分析、变换和优化。例如，`clang` 有一个相当[重量级的机制](https://clang.llvm.org/doxygen/classclang_1_1TreeTransform.html)来执行 C++ 模板实例化。

我们将编译器变换分为两类：局部变换和全局变换。在本章中，我们将重点介绍如何利用 Toy 方言及其高层语义来执行那些在 LLVM 中难以完成的局部模式匹配变换。为此，我们将使用 MLIR 的[通用 DAG 重写器（Generic DAG Rewriter）](../../PatternRewriter.md)。

有两种方法可以实现模式匹配变换：1. 命令式，使用 C++ 编写模式匹配与重写；2. 声明式，使用表驱动的[声明式重写规则（Declarative Rewrite Rules, DRR）](../../DeclarativeRewrites.md)。请注意，使用 DRR 要求操作必须使用 ODS 定义，如[第2章](Ch_02_生成基础MLIR.md)所述。

## 使用 C++ 风格模式匹配与重写优化 Transpose

让我们从一个简单的模式开始，尝试消除一对互为抵消的转置操作：`transpose(transpose(X)) -> X`。以下是相应的 Toy 示例：

```toy
def transpose_transpose(x) {
  return transpose(transpose(x));
}
```

对应以下 IR：

```mlir
toy.func @transpose_transpose(%arg0: tensor<*xf64>) -> tensor<*xf64> {
  %0 = toy.transpose(%arg0 : tensor<*xf64>) to tensor<*xf64>
  %1 = toy.transpose(%0 : tensor<*xf64>) to tensor<*xf64>
  toy.return %1 : tensor<*xf64>
}
```

这是一个很好的例子，说明了在 Toy IR 上匹配起来很简单的变换，但让 LLVM 来搞明白却相当困难。例如，今天的 Clang 无法优化掉那个临时数组，而朴素转置的计算被表达为以下循环：

```c++
#define N 100
#define M 100

void sink(void *);
void double_transpose(int A[N][M]) {
  int B[M][N];
  for(int i = 0; i < N; ++i) {
    for(int j = 0; j < M; ++j) {
       B[j][i] = A[i][j];
    }
  }
  for(int i = 0; i < N; ++i) {
    for(int j = 0; j < M; ++j) {
       A[i][j] = B[j][i];
    }
  }
  sink(A);
}
```

对于涉及在 IR 中匹配树状模式并用一组不同的操作替换它的简单 C++ 重写方法，我们可以通过实现 `RewritePattern` 来接入 MLIR 的 `Canonicalizer` pass：

```c++
/// Fold transpose(transpose(x)) -> x
struct SimplifyRedundantTranspose : public mlir::OpRewritePattern<TransposeOp> {
  /// We register this pattern to match every toy.transpose in the IR.
  /// The "benefit" is used by the framework to order the patterns and process
  /// them in order of profitability.
  SimplifyRedundantTranspose(mlir::MLIRContext *context)
      : OpRewritePattern<TransposeOp>(context, /*benefit=*/1) {}

  /// This method is attempting to match a pattern and rewrite it. The rewriter
  /// argument is the orchestrator of the sequence of rewrites. It is expected
  /// to interact with it to perform any changes to the IR from here.
  llvm::LogicalResult
  matchAndRewrite(TransposeOp op,
                  mlir::PatternRewriter &rewriter) const override {
    // Look through the input of the current transpose.
    mlir::Value transposeInput = op.getOperand();
    TransposeOp transposeInputOp = transposeInput.getDefiningOp<TransposeOp>();

    // Input defined by another transpose? If not, no match.
    if (!transposeInputOp)
      return failure();

    // Otherwise, we have a redundant transpose. Use the rewriter.
    rewriter.replaceOp(op, {transposeInputOp.getOperand()});
    return success();
  }
};
```

此重写器的实现在 `ToyCombine.cpp` 中。[规范化 pass（Canonicalization Pass）](../../Canonicalization.md) 以贪婪、迭代的方式应用操作定义的变换。为了确保规范化 pass 应用我们的新变换，我们设置 [hasCanonicalizer = 1](../../DefiningDialects/Operations.md/#hascanonicalizer) 并将模式注册到规范化框架中。

```c++
// Register our patterns for rewrite by the Canonicalization framework.
void TransposeOp::getCanonicalizationPatterns(
    RewritePatternSet &results, MLIRContext *context) {
  results.add<SimplifyRedundantTranspose>(context);
}
```

我们还需要更新主文件 `toyc.cpp`，添加优化流水线。在 MLIR 中，优化通过 `PassManager` 运行，方式与 LLVM 类似：

```c++
  mlir::PassManager pm(module->getName());
  pm.addNestedPass<mlir::toy::FuncOp>(mlir::createCanonicalizerPass());
```

最后，我们可以运行 `toyc-ch3 test/Examples/Toy/Ch3/transpose_transpose.toy -emit=mlir -opt` 并观察我们的模式运行：

```mlir
toy.func @transpose_transpose(%arg0: tensor<*xf64>) -> tensor<*xf64> {
  %0 = toy.transpose(%arg0 : tensor<*xf64>) to tensor<*xf64>
  toy.return %arg0 : tensor<*xf64>
}
```

不出所料，我们现在直接返回了函数参数，绕过了转置操作。然而，其中一个转置仍然没有被消除。这不太理想！发生的情况是，我们的模式用函数输入替换了最后一个转置，而留下了现在已死的转置输入。Canonicalizer 知道如何清理死操作；然而，MLIR 保守地认为操作可能具有副作用。我们可以通过向 `TransposeOp` 添加一个新的 trait —— `Pure` 来解决这个问题：

```tablegen
def TransposeOp : Toy_Op<"transpose", [Pure]> {...}
```

现在重新尝试 `toyc-ch3 test/transpose_transpose.toy -emit=mlir -opt`：

```mlir
toy.func @transpose_transpose(%arg0: tensor<*xf64>) -> tensor<*xf64> {
  toy.return %arg0 : tensor<*xf64>
}
```

完美！没有任何 `transpose` 操作留下——代码已经是最优的了。

在下一节中，我们将使用 DRR 对 Reshape 操作进行模式匹配优化。

## 使用 DRR 优化 Reshape

声明式、基于规则的模式匹配与重写（DRR）是一种基于操作 DAG 的声明式重写器，为模式匹配和重写规则提供了基于表的语法：

```tablegen
class Pattern<
    dag sourcePattern, list<dag> resultPatterns,
    list<dag> additionalConstraints = [],
    dag benefitsAdded = (addBenefit 0)>;
```

一个类似于 SimplifyRedundantTranspose 的冗余 reshape 优化可以使用 DRR 更简洁地表达：

```tablegen
// Reshape(Reshape(x)) = Reshape(x)
def ReshapeReshapeOptPattern : Pat<(ReshapeOp(ReshapeOp $arg)),
                                   (ReshapeOp $arg)>;
```

每个 DRR 模式对应的自动生成的 C++ 代码可以在 `path/to/BUILD/tools/mlir/examples/toy/Ch3/ToyCombine.inc` 中找到。

DRR 还提供了一种在变换以参数和结果的某些属性为条件时添加参数约束的方法。一个例子是当 reshape 冗余时（即输入和输出形状相同时）消除 reshape 的变换。

```tablegen
def TypesAreIdentical : Constraint<CPred<"$0.getType() == $1.getType()">>;
def RedundantReshapeOptPattern : Pat<
  (ReshapeOp:$res $arg), (replaceWithValue $arg),
  [(TypesAreIdentical $res, $arg)]>;
```

某些优化可能需要对指令参数进行额外的变换。这是通过 NativeCodeCall 实现的，它可以通过调用 C++ 辅助函数或使用内联 C++ 来实现更复杂的变换。这种优化的一个例子是 FoldConstantReshape，我们通过原地 reshape 常量值并消除 reshape 操作来优化 Reshape of Constant。

```tablegen
def ReshapeConstant : NativeCodeCall<"$0.reshape(($1.getType()).cast<ShapedType>())">;
def FoldConstantReshapeOptPattern : Pat<
  (ReshapeOp:$res (ConstantOp $arg)),
  (ConstantOp (ReshapeConstant $arg, $res))>;
```

我们使用以下 trivial_reshape.toy 程序来演示这些 reshape 优化：

```c++
def main() {
  var a<2,1> = [1, 2];
  var b<2,1> = a;
  var c<2,1> = b;
  print(c);
}
```

```mlir
module {
  toy.func @main() {
    %0 = toy.constant dense<[1.000000e+00, 2.000000e+00]> : tensor<2xf64>
    %1 = toy.reshape(%0 : tensor<2xf64>) to tensor<2x1xf64>
    %2 = toy.reshape(%1 : tensor<2x1xf64>) to tensor<2x1xf64>
    %3 = toy.reshape(%2 : tensor<2x1xf64>) to tensor<2x1xf64>
    toy.print %3 : tensor<2x1xf64>
    toy.return
  }
}
```

我们可以尝试运行 `toyc-ch3 test/Examples/Toy/Ch3/trivial_reshape.toy -emit=mlir -opt` 并观察我们的模式运行：

```mlir
module {
  toy.func @main() {
    %0 = toy.constant dense<[[1.000000e+00], [2.000000e+00]]> : tensor<2x1xf64>
    toy.print %0 : tensor<2x1xf64>
    toy.return
  }
}
```

不出所料，规范化之后没有留下任何 reshape 操作。

关于声明式重写方法的更多细节可以在 [Table-driven Declarative Rewrite Rule (DRR)](../../DeclarativeRewrites.md) 中找到。

在本章中，我们看到了如何通过始终可用的钩子来使用某些核心变换。在[下一章](Ch_04_使用Interfaces开启通用变换.md)中，我们将看到如何通过 Interface 来使用更具扩展性的通用解决方案。
